// src/utils/excursions.js
export const weekdayShort = (t, idx) => t(`exc.weekdays_short.${idx}`);
export const weekdayLong  = (t, idx) => t(`exc.weekdays_long.${idx}`);
export const langName     = (t, code) => t(`exc.lang_name.${code}`);
